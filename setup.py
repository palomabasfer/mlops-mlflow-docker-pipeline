from setuptools import find_packages, setup

setup(
    name="mlops_pipeline",
    version="0.1.0",
    description="Production MLOps Pipeline with MLflow & Docker",
    author="Paloma Bas Fernández",
    packages=find_packages(),
)
